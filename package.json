{
  "name": "simple-irc-client-website",
  "description": "Simple Irc Client",
  "version": "1.0.0",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "npx tailwindcss -i ./dist/app.css -o ./dist/css/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "tailwindcss": "^3.4.13"
  }
}